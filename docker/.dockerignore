# Docker chỉ đọc .dockerignore tại thư mục BUILD CONTEXT (thường là root repo).
# Giữ danh sách trùng root `.dockerignore` phòng ai đặt context nhầm thư mục `docker/`.
node_modules
dist
.git
.gitignore
*.log
.env.local
.env.development
.env.production
.DS_Store
.cursor
